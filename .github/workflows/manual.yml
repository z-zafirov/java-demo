name: Run Postman Collection

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options: 
        - monalisa
        - cschleiden
      message:
          required: true
      use-emoji:
          type: boolean
          description: Include 🎉🤣 emojis
      environment:
          type: environment

jobs:
  newman:
    runs-on: ubuntu-latest
    
    steps:
      - name: Run Postman Collection
        run: newman run https://raw.githubusercontent.com/z-zafirov/postman/master/postman.sample.json

      # - uses: actions/checkout@master
      # - uses: matt-ball/newman-action@master
        # with:
          # apiKey: ${{ secrets.postmanApiKey }}
          # collection: https://github.com/z-zafirov/postman/blob/master/postman.sample.json
          # environment: 5922408-228c7edd-fc15-4f68-9665-a35d7df6945b
